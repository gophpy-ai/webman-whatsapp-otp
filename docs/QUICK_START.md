# Quick Start

- Ensure your Webman project is using composer and has `support/cache` enabled (or Redis).
- Install plugin via composer or copy plugin files into project.
- Configure `config/plugin/whatsapp-otp/config.php`.
- Register WhatsApp template `otp_login_code` in Meta Business Manager.
